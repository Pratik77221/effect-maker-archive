#!/bin/sh
set -eu
EM_SYNC_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if ! command -v node >/dev/null 2>&1; then
  echo 'Node.js 22 or newer is required on PATH.' >&2
  exit 1
fi
exec node "$EM_SYNC_DIR/cli.js" "$@"
